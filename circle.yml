machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t sidewalklabs/valhalla:$CIRCLE_SHA1 .

test:
  override:
    - docker run -it -v $PWD/tyr/test/data:/valhalla/maps -v $PWD/data_valhalla:/data/valhalla valhalla pbfgraphbuilder -c conf/valhalla.json maps/liechtenstein-latest.osm.pbf
    - docker run -d -p 8002:8002 sidewalklabs/valhalla:$CIRCLE_SHA1; sleep 10
    - curl -v http://localhost:8002
    - curl -v http://localhost:8002/route?json=%7B%22locations%22%3A%5B%7B%22lat%22%3A47.14530%2C%22lon%22%3A9.51976%7D%2C%7B%22lat%22%3A47.17051%2C%22lon%22%3A9.51703%7D%5D%2C%22costing%22%3A%22auto%22%7D

deployment:
  master:
    branch: master
    commands:
      - docker tag sidewalklabs/valhalla:$CIRCLE_SHA1 sidewalklabs/valhalla:master
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push sidewalklabs/valhalla
